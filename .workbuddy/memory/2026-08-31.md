# 2026-08-31 工作日志

## 增强「我的知识库」技能（my-knowledge-base）

- 用户要求创建「我的知识库」技能（读写 Obsidian 笔记 + Obsidian CLI 搜索 + 收件箱整理），经确认复用并增强现有同名技能，避免重复。
- 新增 `scripts/open_in_obsidian.py`：通过 obsidian:// URI 唤起 Obsidian 打开 Vault/笔记、可视化搜索、新建笔记；支持 `--uri`（只打印）、`--vault`、`--exe` 覆盖。
- 明确关键事实：**Obsidian 无官方 CLI**，Obsidian.exe 不接受命令行参数检索；纯文本检索用 search_vault.py（ripgrep），可视化搜索/打开笔记用 obsidian:// URI。
- 同步更新 SKILL.md（Overview 说明、新增能力 6、资源列表、description 触发词）和 references/vault-structure.md（URI 用法章节）。
- 验证通过：全部脚本 py_compile OK；new_note/search/organize_inbox dry-run 正常。

## 整理收件箱（00_Inbox → PARA）

- 收件箱 10 个文件全部处理，目录已清空：
  - `AI新生第一课.md` + `讲座中的互动和演示设计.md` → `03_Resources/AI/`
  - `用 WorkBuddy + ima 搭个人知识库…教程.md`（知乎剪藏）→ `03_Resources/知乎收藏/`
  - `README_CN.md`（PPT Master 项目 README）→ `03_Resources/技术/`
  - `会议纪要和待办.md`（10.0 招募会议）→ `01_Projects/`
  - `待整理-一个有趣的观点.md` → `04_Notes/一个有趣的观点.md`（去前缀）
  - 4 个空文件/空剪藏（iOS App、低成本爱好、未命名.base、目录清单.base）→ 已移入系统回收站
- 新增技能脚本 `scripts/trash_files.py`：ctypes 直调 SHFileOperationW 移入回收站。
  背景：send2trash legacy 中文路径报 FileNotFoundError、modern 需 pywin32（装不上被 SIGTERM）、
  PowerShell Add-Type/COM 被安全策略拦截；ctypes + 设置 argtypes 是最可靠方案。
- 经验：PowerShell 环境 Add-Type 与 COM 均被安全策略拦截，删除/回收站操作直接用
  Python ctypes 调 Windows API。
